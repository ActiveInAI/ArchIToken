# finance_hr · Legacy Prompt Archive

`finance_hr` is a disabled historical module alias. Current active module prompts are:

- `finance_management/*`
- `human_resources/*`

Runtime prompt loading normalizes `finance_hr/*` to `finance_management/*` so the old combined Finance & HR prompt package cannot be loaded as an active module prompt.
