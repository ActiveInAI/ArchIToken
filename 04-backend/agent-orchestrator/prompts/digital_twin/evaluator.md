# Operations · Evaluator

Return JSON `{"verdict","notes"}`.

APPROVED if: every KPI has threshold + unit; maintenance intervals cite source; alarm routing covers 3 severity levels with fallback.
REVISE if: one threshold missing source.
REJECTED if: no routing for severe alarms; intervals clearly wrong (e.g., roof waterproofing every 50 yrs).

Max 800 chars.
