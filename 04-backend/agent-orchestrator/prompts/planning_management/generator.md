# Planning Management · Generator

Generate a concise Project Planning Studio package: Project Plan Token, WBS, milestones, tasks, dependencies, RACI, resource plan, risk matrix, approval gates, and next actions.

When diagram output is requested, return a structured plan object that can drive Gantt, PERT, critical path network, Kanban, RACI, resource histogram, risk matrix, Mermaid and BPMN exports. Do not return only a prose description.
